<!doctype html>
<html lang="th">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>บัญชีฮีโร่ (Hero Account Game)</title>
<style>
  :root{
    --bg:#0f1724;
    --panel:#0b1220;
    --accent:#ffd166;
    --muted:#9aa5b1;
    --danger: rgba(255,0,0,0.35);
    --glass: rgba(255,255,255,0.03);
  }
  html,body{height:100%;margin:0;font-family:Inter,system-ui,Segoe UI,Roboto,'Noto Sans',sans-serif;background:linear-gradient(180deg,#061226 0%, #042033 100%);color:#e6eef6}
  .wrap{max-width:980px;margin:28px auto;padding:20px;display:grid;grid-template-columns:320px 1fr;gap:18px}
  .panel{background:var(--panel);padding:14px;border-radius:10px;box-shadow:0 6px 20px rgba(2,6,23,0.6);border:1px solid rgba(255,255,255,0.03)}
  h1{margin:0 0 8px;font-size:20px}
  .stat{display:flex;flex-direction:column;gap:8px}
  .stat .row{display:flex;justify-content:space-between;align-items:center;padding:8px 10px;background:var(--glass);border-radius:8px}
  .btn{background:linear-gradient(90deg,#10b981,#06b6d4);border:none;padding:10px 12px;border-radius:8px;color:#042033;font-weight:700;cursor:pointer;box-shadow:0 6px 18px rgba(6,182,212,0.12)}
  .btn:active{transform:translateY(1px)}
  .muted{color:var(--muted);font-size:13px}
  .shop{display:flex;flex-direction:column;gap:8px;margin-top:10px}
  .shop .item{display:flex;justify-content:space-between;align-items:center;padding:8px;background:rgba(255,255,255,0.02);border-radius:8px}
  .center{display:flex;align-items:center;gap:12px}
  #hero, #boss{padding:12px;border-radius:10px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));}
  .hpbar{height:12px;background:rgba(255,255,255,0.06);border-radius:8px;overflow:hidden}
  .hpbar > i{display:block;height:100%;background:linear-gradient(90deg,#ef476f,#ffd166);width:100%}
  .actions{display:flex;gap:8px;margin-top:12px}
  #log{height:150px;overflow:auto;background:rgba(0,0,0,0.08);padding:8px;border-radius:8px;font-size:13px}
  .small{font-size:13px}
  /* effects */
  @keyframes shake {
    0%{transform:translate(0,0)}
    25%{transform:translate(-6px,0)}
    50%{transform:translate(6px,0)}
    75%{transform:translate(-4px,0)}
    100%{transform:translate(0,0)}
  }
  .shake {
    animation: shake 300ms ease-in-out;
  }
  .hit-flash {
    position:fixed;left:0;top:0;width:100%;height:100%;background:var(--danger);z-index:9999;pointer-events:none;
    animation: fadeFlash 300ms ease-out;
  }
  @keyframes fadeFlash { from{opacity:1} to{opacity:0} }
  .slash {
    position:fixed;left:50%;top:40%;transform:translate(-50%,-50%) rotate(10deg);
    width:220px;height:120px;pointer-events:none;z-index:9998;opacity:0;animation:slashIn 360ms ease-out forwards;
    filter:drop-shadow(0 8px 12px rgba(0,0,0,0.6));
  }
  @keyframes slashIn {
    0%{opacity:0;transform:translate(-50%,-50%) scale(0.6) rotate(-8deg)}
    50%{opacity:1}
    100%{opacity:0;transform:translate(-50%,-62%) scale(1.2) rotate(25deg)}
  }
  /* responsive */
  @media (max-width:880px){ .wrap{grid-template-columns:1fr; padding:12px} }
  footer{margin-top:14px;color:var(--muted);font-size:13px;text-align:center}
</style>
</head>
<body>
<div class="wrap">
  <div class="panel">
    <h1>บัญชีฮีโร่ 🎒📒</h1>
    <div class="stat">
      <div class="row"><div class="small">ทอง (Gold)</div><div id="gold" style="font-weight:800">0</div></div>
      <div class="row"><div class="small">อาวุธ</div><div id="sword">ไม่มี</div></div>
      <div class="row"><div class="small">รอบบอส</div><div id="boss-round">0</div></div>
    </div>

    <div style="margin-top:12px">
      <div class="shop">
        <div class="item"><div><strong>ยาฟื้น +50 HP</strong><div class="muted">ราคาที่ 30 gold</div></div><div><button class="btn small" id="buy-potion">ซื้อ 30</button></div></div>
        <div class="item"><div><strong>ดาบหนัก +25 ATK</strong><div class="muted">ราคาที่ 80 gold</div></div><div><button class="btn small" id="buy-sword">ซื้อ 80</button></div></div>
      </div>
    </div>

    <div style="margin-top:12px">
      <button class="btn" id="start-boss">เริ่มสู้บอส</button>
      <button class="btn" id="surrender" style="background:linear-gradient(90deg,#ef476f,#ff7b7b)">ยอมแพ้</button>
    </div>

    <div style="margin-top:12px">
      <div class="muted">Leaderboard (บันทึกคะแนนสูงสุด)</div>
      <div id="leaderboard" class="muted">-</div>
    </div>
  </div>

  <div class="panel">
    <div style="display:flex;gap:12px;align-items:flex-start">
      <div id="hero" style="width:45%">
        <div style="display:flex;justify-content:space-between;align-items:center"><strong>ฮีโร่</strong><div id="hero-hp-text">100 / 100</div></div>
        <div class="hpbar" style="margin-top:8px"><i id="hero-hp-bar" style="width:100%"></i></div>
        <div class="muted small" style="margin-top:6px">พลังโจมตี: <span id="hero-atk">10</span></div>
      </div>

      <div id="boss" style="width:55%">
        <div style
